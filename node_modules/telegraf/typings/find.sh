files=$(ls ./)
echo "$1"
for file in $files; do
    if [ "$file" == "find.sh" ]; then
        continue
    fi
    echo -e "\tIn $file:"
    find_result=$(cat -n $file | grep "$1")
    if [ -z "$find_result" ]; then
        echo "No match in $file"
    else
        echo "$find_result"
    fi
done
